# API_URL = server-side Strapi (production: http://127.0.0.1:1337)
# NEXT_PUBLIC_API_URL = optional override for media in the browser.
#   Leave empty in production so /uploads is proxied by Next.js.
#   Locally you can set http://localhost:1337 or leave empty (rewrite still works).
API_URL=http://localhost:1337
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_SITE_URL=https://zibamag.ir
PREVIEW_SECRET=
GA_MEASUREMENT_ID=
